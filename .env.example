## Required secrets (export before running)
VOYAGE_API_KEY=
ES_URL=
OPENAI_API_KEY=

## Note
# - Not auto-loaded by default. Export these in your shell, e.g.:
#     set -a; source .env; set +a
# - Or opt-in auto-load by setting RAGGS_LOAD_DOTENV=1 (requires python-dotenv).
# - All other tuning (models, batch sizes, ranking knobs, etc.)
#   lives in YAML under configs/ (see configs/default.yaml and configs/local.yaml).
